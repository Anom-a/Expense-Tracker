# 💰 Java Servlet Expense Tracker

![Java](https://img.shields.io/badge/Java-17%2B-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=for-the-badge&logo=postgresql&logoColor=white)
![Maven](https://img.shields.io/badge/Maven-C71A36?style=for-the-badge&logo=apachemaven&logoColor=white)
![Jakarta EE](https://img.shields.io/badge/Jakarta%20EE-Servlets%206.0-F3B411?style=for-the-badge&logo=jakartaee&logoColor=black)

A **multi-user expense tracking web application** built using **Java Servlets (Jakarta EE)**, **JSP**, and **PostgreSQL**.

This project focuses on **core backend web fundamentals**—authentication, session handling, JDBC, and the DAO pattern—without relying on heavy frameworks like Spring.

---

## ✨ Key Features

- 🔐 **Authentication** – User registration, login, and logout  
- 👥 **Multi-user system** – Each user sees only their own expenses  
- 📝 **Expense management** – Add expenses with category, amount, and date  
- 📊 **Expense history** – Chronological view of all expenses  
- 🔌 **PostgreSQL + JDBC** – Direct database interaction  
- 🏗 **MVC Architecture** – Clean separation of concerns  

---

## 🛠 Tech Stack

| Layer | Technology |
|------|-----------|
| Backend | Java 17, Jakarta Servlets 6.0 |
| Frontend | JSP, HTML5, CSS3 |
| Database | PostgreSQL |
| Build Tool | Apache Maven |
| Server | Jetty (Maven Plugin) |

---

## 📂 Project Structure
```
src/main/java/com/example
├── dao/ # Database access (SQL logic)
│ ├── UserDAO.java
│ └── ExpenseDAO.java
├── model/ # Data models (POJOs)
│ ├── User.java
│ └── Expense.java
├── util/ # Utilities
│ └── DBUtil.java # JDBC connection factory
└── web/ # Servlets (Controllers)
├── LoginServlet.java
├── RegisterServlet.java
├── LogoutServlet.java
└── ExpenseController.java
```

---

## 🚀 Getting Started

### 1️⃣ Prerequisites

Ensure the following are installed:

- Java JDK **17+**
- Apache Maven
- PostgreSQL (running locally)

---

### 2️⃣ Database Setup

Run the following SQL commands using `psql` or pgAdmin:

```sql
CREATE DATABASE testdb;
\c testdb;

CREATE TABLE users (
  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,
  role VARCHAR(50) NOT NULL DEFAULT 'USER'
);

CREATE TABLE expenses (
  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id INT NOT NULL,
  category VARCHAR(50),
  amount DECIMAL(10,2) NOT NULL,
  expense_date DATE NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```
### 3️⃣ Configure Database Connection

Edit the database credentials in:
src/main/java/com/example/util/DBUtil.java
```
private static final String URL = "jdbc:postgresql://localhost:5432/testdb";
private static final String USER = "postgres";      // your DB username
private static final String PASSWORD = "password";  // your DB password
```
### 4️⃣ Build and Run the Application

Use the Jetty Maven plugin:
```
# Build the project
mvn clean install

# Start the server
mvn jetty:run
```
### 5️⃣ Access the Application
```
http://localhost:8080/expenses
```
