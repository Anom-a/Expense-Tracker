# 💰 Java Servlet Expense Tracker

![Java](https://img.shields.io/badge/Java-17%2B-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=for-the-badge&logo=postgresql&logoColor=white)
![Maven](https://img.shields.io/badge/Maven-C71A36?style=for-the-badge&logo=apachemaven&logoColor=white)
![Jakarta EE](https://img.shields.io/badge/Jakarta%20EE-F3B411?style=for-the-badge&logo=jakartaee&logoColor=black)

A robust, multi-user **Expense Tracking Application** built from scratch using **Java Servlets (Jakarta EE)**, **JSP**, and **PostgreSQL**. This project demonstrates core web development concepts including authentication, session management, JDBC connectivity, and the DAO pattern without relying on heavy frameworks like Spring.

---

## ✨ Features

*   **🔐 User Authentication:** Secure Login and Registration system.
*   **👥 Multi-User Support:** Data isolation—users only see their own expenses.
*   **📝 Expense Management:** Add expenses with categories, amounts, and dates.
*   **📊 History View:** View a chronological list of all past expenses.
*   **🔌 Database Driven:** Fully integrated with PostgreSQL using JDBC.
*   **🏗 MVC Architecture:** Clean separation of concerns (Model, View, Controller, DAO).

---

## 🛠 Tech Stack

| Component | Technology |
| :--- | :--- |
| **Backend** | Java 17, Jakarta Servlets 6.0 |
| **Frontend** | JSP, HTML5, CSS3 |
| **Database** | PostgreSQL |
| **Build Tool** | Apache Maven |
| **Server** | Jetty (via Maven Plugin) |

---

## 📂 Project Structure

```text
src/main/java/com/example
├── dao/          # Data Access Objects (SQL Logic)
│   ├── UserDAO.java
│   └── ExpenseDAO.java
├── model/        # POJOs (Plain Old Java Objects)
│   ├── User.java
│   └── Expense.java
├── util/         # Utilities
│   └── DBUtil.java (Connection Factory)
└── web/          # Servlets (Controllers)
    ├── LoginServlet.java
    ├── RegisterServlet.java
    ├── LogoutServlet.java
    └── ExpenseController.java

🚀 Getting Started

Follow these instructions to get the project up and running on your local machine.
1. Prerequisites

    Java JDK 17 or higher

    Apache Maven

    PostgreSQL installed and running

2. Database Setup

Open your terminal or pgAdmin and run the following SQL commands to initialize the schema:
code SQL

    
CREATE DATABASE testdb;

\c testdb;

CREATE TABLE "users" (
  "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  "name" VARCHAR(100) NOT NULL,
  "email" VARCHAR(100) UNIQUE NOT NULL,
  "password" VARCHAR(255) NOT NULL,
  "role" VARCHAR(100) NOT NULL DEFAULT 'USER'
);

CREATE TABLE "expenses" (
  "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  "user_id" INT NOT NULL,
  "category" VARCHAR(50),
  "amount" DECIMAL(10,2) NOT NULL,
  "expense_date" DATE NOT NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id")
);

  

3. Configure Application

Navigate to src/main/java/com/example/util/DBUtil.java and update your database credentials:
code Java

    
private static final String URL = "jdbc:postgresql://localhost:5432/testdb";
private static final String USER = "postgres";  // YOUR_DB_USERNAME
private static final String PASSWORD = "password"; // YOUR_DB_PASSWORD

  

4. Build and Run

We use the Jetty Maven Plugin for a lightweight development experience.
code Bash

    
# Clean and compile
mvn clean install

# Run the server
mvn jetty:run

  

Once the server starts, open your browser and visit:

👉 http://localhost:8080/expenses

(You will be redirected to the Login page automatically).